#same like as list but tuple in immutable 
#tuple is immutable and value can't be changed

tuple = (1,2,3,4,5,6); #syntax of the tuple

print(tuple);
print(tuple[3:6])
# tuple[2:3] = 6; #'tuple' object does not support item assignment

print(tuple);
